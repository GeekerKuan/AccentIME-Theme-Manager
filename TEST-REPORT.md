# AccentIME Theme Manager 0.5.0 Preview 测试记录

日期：2026-09-05  
平台：Windows x64，微信输入法 2.1.3.18

## 自动验证

- MSVC Release x64 编译通过，`/W4 /WX /permissive- /O2 /GL /LTCG`。
- 编译为 0 个警告、0 个错误。
- 原生核心测试通过：颜色解析、颜色编码和 WinUI 3 风格语音材质色生成。
- 使用本机正式版 `app.so` 备份副本生成 Windows 蓝 `#0078D4` 补丁并验证。
- 补丁文件与原文件等长，且只允许 3 个颜色字段和 3 个识别气泡专用字段变化。
- Windows 蓝对应的语音材质渐变为 `#273D4E → #202C36`；原白色图案、音量动画及完成提示没有修改。
- 识别气泡背景改用同一强调色深色材质，圆角从 20 调整为 12；独立尾部绘制函数返回 Dart `Null`，不进入原三角形绘制路径。
- 补丁后的 x64 指令反汇编通过：颜色函数返回合法对象池 `Color` 对象，圆角仍从合法 double 池项读取，尾部 painter 返回合法 Dart `Null`。
- 兼容识别 0.2.x 的“仅候选项颜色”及 0.3/0.4 的语音背景状态，可继续应用新方案或恢复原文件。
- WiX 6.0.2 MSI 构建通过。

## 性能快照

- EXE：约 743 KB。
- 窗口首次出现：约 152 ms。
- 前台空闲工作集：约 8.8 MB。
- 私有内存：约 1.6 MB。
- 启动阶段 CPU：约 0.03 秒。

以上为此前同一原生程序架构的单次 Release 构建测量，不作为跨机器性能承诺。语音界面由微信原进程绘制，不增加运行时进程；自动跟随使用注册表事件阻塞等待，不轮询。

## 尚需人工验证

未替换当前运行版微信输入法。识别气泡实际布局、动态圆点、完成提示、系统色变化后的输入法恢复和卸载恢复需由用户按 README 的步骤验证。
